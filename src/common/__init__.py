"""Common (shared kernel).

Cross-cutting primitives shared by the other packages (identifiers, clocks,
shared error bases) will land here as concrete needs appear -- deliberately
empty for now to avoid premature abstraction.
"""
